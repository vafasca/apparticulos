@model IEnumerable<ArticulosWeb.Models.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Categoria</h1>

<p>
    <a asp-action="Create">Agregar Categoria</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCategoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCategoria)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.CategoriaId">Edit</a> |*@
                <a asp-action="Details" asp-route-id="@item.CategoriaId">Detalle</a>
                @*<a asp-action="Delete" asp-route-id="@item.CategoriaId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
